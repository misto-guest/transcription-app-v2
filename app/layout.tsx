import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Transcription App - YouTube, Spotify & Audio File Transcripts",
  description: "Extract transcripts from YouTube, download and transcribe Spotify audio, or upload any audio file for AI-powered transcription using AssemblyAI",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
